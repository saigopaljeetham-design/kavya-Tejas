"use client";

/**
 * Mandap experience intentionally removed from the invitation.
 * The main experience now flows directly from the occasion section
 * into the Save the Date / cinematic closing sequence.
 */
export function MandapScene() {
  return null;
}
