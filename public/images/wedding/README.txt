Project-2 wedding photo assets belong in this directory.

The gallery configuration on branch `agent/add-kavya-tejas-photos` references the ten Kavya & Tejas photos added for the invitation. The binary JPG upload is packaged separately because the connected GitHub file tool cannot accept local binary files directly.
